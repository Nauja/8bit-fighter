extends Node

enum EDirection { LEFT, RIGHT, UP, DOWN }

enum EEquipmentSlot { HEAD, WEAPON, ARMOR, FOOT }

enum EActorState { Move, Hit, Attack, Lift, Lifted, Thrown, Chase }

enum ENetworkInterfaceState { CLOSED, HOSTING, JOINING, JOINED }
